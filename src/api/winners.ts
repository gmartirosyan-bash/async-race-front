// import type { Winner } from "../types/types";

// const URL = 'http://localhost:3000/winners';
//
// const getWinnersApi = async (page: number): Promise<winners: Winner[]> => {

// };

// const addWinnerApi = async (carObj: { name: string; color: string }): Promise<Car> => {

// };

// export default { getWinnersApi };
